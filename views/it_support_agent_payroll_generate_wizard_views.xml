<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_it_support_agent_payroll_generate_wizard_form" model="ir.ui.view">
        <field name="name">it.support.agent.payroll.generate.wizard.form</field>
        <field name="model">it.support.agent.payroll.generate.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="month"/>
                    <field name="year"/>
                    <field name="agent_ids" widget="many2many_tags"/>
                </group>
                <footer>
                    <button name="action_generate" string="Generate" type="object" class="oe_highlight"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_it_support_agent_payroll_generate_wizard" model="ir.actions.act_window">
        <field name="name">Generate Monthly Payroll</field>
        <field name="res_model">it.support.agent.payroll.generate.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
